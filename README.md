# DTR Formatter

A local website for converting an exported DTR Excel record (`.xls`) into a relayouted attendance workbook (`.xlsx`).

## Run

```bash
./run.sh
```

Open the local address shown in the terminal, upload the DTR record, generate the relayouted workbook, then download the Excel file.
